# Sesión 14-Jun-2026 — Resumen de trabajo

> Documento generado al cierre de la sesión. Commits `c8f2ef0` → `66f9048`.

---

## 1. VOID_GLITCH — Integración en el OS

**Problema:** El juego existía completo (frontend + API) pero estaba enterrado bajo `EXPERIENCIAS_GENERATIVAS`. Sin ícono en escritorio, sin acceso rápido en Inicio, sin comando en VOMIT.SH.

**Commits:** `c8f2ef0`

| Cambio | Archivo | Línea |
|--------|---------|-------|
| `openVoidGlitch()` con ventana 600x720 | `index.html` | 756 |
| Ícono en escritorio `🕳 VOID_GLITCH` | `index.html` | 1459-61 |
| Comandos VOMIT.SH: `void`, `glitch`, `voidglitch`, `caida`, `caída` | `index.html` | 1250 |
| Mapa `SPECIALS` en terminal | `index.html` | 1270 |
| Documentación en `help` | `index.html` | 1285 |
| Casos en `dispatch` | `index.html` | 1349-50 |
| Acceso directo en Menú Inicio | `index.html` | 1517 |

---

## 2. VOID_GLITCH — Identidad de modelos visible

**Problema:** El juego no mostraba qué modelo de IA estaba corriendo. Status line solo decía el modo.

**Commit:** `e10defc`

| Elemento | Antes | Ahora |
|----------|-------|-------|
| Kicker | Siempre "esquizoai / question engine..." | `MSN · PSYCHO_BOT · Llama 3.3 70B` / `TERMINAL · Modelo A // Modelo B` / `STATIC · fallback pre-escrito · offline` |
| Status bar | Solo modo + contadores | Incluye nombre del modelo activo (color por modo) |
| Typing indicator | "PSYCHO_BOT está mutando..." | `PSYCHO_BOT · Llama 3.3 70B está generando...` |
| Fallback silencioso | Se volvía a static sin avisar | Mensaje explícito: *"API caída · Llama 3.3 70B no responde → usando fallback estático. Verifica las API keys en Vercel"* |

---

## 3. API void-glitch.js — Syntax error que rompía el build

**Problema:** Template literal mal cerrado (`\`` escapado) metía todo el código restante dentro del string. Vercel fallaba el build → producción congelada.

**Commit:** `7ae68b2`

```js
// ROTO (línea 222):
...choices.`\n    }\n  ];\n\n  let raw = '';\n  try {\n    raw = await callModel(...

// CORREGIDO:
...choices.`,
    },
  ];

  let raw = '';
  try {
    raw = await callModel(...
```

---

## 4. VOID_GLITCH — Motor de audio oracular

**Problema:** El vacío era silencioso.

**Commits:** `2c508f0`, `6e126a8` (fix TDZ `let`→`var`), `7cfa88d` (rediseño)

### Capas de audio:

| Capa | Descripción |
|------|-------------|
| **Drone** | 2 sawtooths desafinados (55Hz) → waveshaper distortion → lowpass filter → delay con feedback. LFO respiratorio modulando el filtro |
| **Drums** | Kick sintetizado (sine 130Hz→35Hz), tempo variable según glitch/caos (2.2s→300ms) |
| **sfxBoot** | Ráfaga noise + sub-bass 28Hz rumble + sweep 180→600Hz + sparkle highpass |
| **sfxChoice** | Gong: 5 armónicos (110, 165, 220, 293, 440Hz) + glitch flicker tail |
| **sfxRender(void)** | Sub-bass 35Hz, swell lento, oscuridad |
| **sfxRender(glitch)** | Noise bandpass Q=20, shimmer metálico |
| **sfxRender(chaos)** | Noise sweep 2000→200Hz, ruidosa, + glitchDrop |
| **sfxTyping** | Campanilla etérea: armónicos 880+1320Hz, muy sutil |
| **sfxFallback** | Distortion x12 + thud 35Hz, la máquina muere |
| **glitchDrop** | En mood chaos: duck extremo del drone, distortion x18, rasgadura noise, doble kick grave |
| **Toggle 🔊/🔇** | Botón en barra de controles, persiste en localStorage |

### Mapeo de medidores al drone:

| Medidor | Efecto |
|---------|--------|
| VOID (depth) ↑ | Baja frecuencia base (55→25Hz), más abisal. LFO más lento |
| GLITCH ↑ | Distortion, resonancia del filtro, detune, LFO más rápido |
| CAOS ↑ | Delay time más corto (errático), más detune, más volumen, LFO extremo |

---

## 5. VOID_GLITCH — UX psycho-oracular

**Commit:** `034bb80`, `154b67f`

### Pulse del vacío mientras carga:
- CSS `voidPulse`: glow morado↔verde en el `#voidBox` durante la generación
- Clase `.void-loading` toggle en `setBusy()` 

### Kicker oracular:
- Mensajes aleatorios mientras la API responde: *"el vacío consulta las líneas mutantes..."*, *"VALIS interfiere la transmisión..."*, *"el hexagrama se quiebra..."*

### Glitch antes del texto:
- El glitch visual se dispara **antes** de setear el texto, no después. El ojo ve: glitch shake → texto nuevo → scroll up

### Pausa dramática:
- `revealLiveNode()`: cuando la API responde, se oculta el typing, hay **500ms de silencio**, y recién ahí aparece la respuesta con glitch + scroll

### Auto-scroll:
- `voidBox.scrollIntoView({behavior:'smooth'})` al renderizar nuevo nodo

---

## 6. Límites de texto ampliados

**Commits:** `034bb80`, `65b391f`

| Parámetro | Antes | Ahora |
|-----------|-------|-------|
| `max_tokens` (API) | 1400 | 2000 |
| `text` (pregunta) | 220 chars | 380 chars |
| `sub` (subtítulo) | 220 chars | 280 chars |
| `log` | 180 chars | 200 chars |
| `label` (choices) | 48 chars | 80 chars |
| `note` (choices) | 80 chars | 120 chars |

---

## 7. Anti-voseo en todos los system prompts

**Commits:** `1db35da`, `66f9048`

Regla explícita en `api/void-glitch.js`, `api/oracle.js` y prompts legacy:

```
PROHIBIDO el voseo argentino: nunca uses "vos", "tenés", "probá", "escribí", "sos", "querés".
Usa "tú", "tienes", "prueba", "escribe", "eres", "quieres".
```

Guardado en memoria: `~/.claude/projects/.../memory/feedback_no_voseo_argentino.md`

---

## 8. REPRODUCTOR — 5 nuevos temas punk

**Commit:** `bcb0b2b`

| # | Artista | Tema | Peso |
|---|---------|------|------|
| 1 | His Hero Is Gone | Like Weeds | 3.2 MB |
| 2 | Antisect | In Darkness | 4.7 MB |
| 3 | Dystopia | Backstabber | 3.0 MB |
| 4 | Subhumans | Mickey Mouse Is Dead | 3.8 MB |
| 5 | Conflict | The Ungovernable Force | 15 MB |

**Fix en `tools/download-music.py`:** Agregado `--extractor-args youtube:player_client=android` (YouTube cambió API, requiere bypass SABR). Removido `--max-filesize 30M` que cortaba tracks largos.

---

## 9. ORACULO.exe — I Ching + VALIS

**Commits:** `78bfd08`, `346e870`, `590305b`, `4d93b83`, `66f9048`

### Arquitectura:

| Capa | Archivo | Descripción |
|------|---------|-------------|
| **API** | `api/oracle.js` | Edge Function. Llama a Groq/DeepSeek, interpreta hexagramas. Mismo patrón que void-glitch |
| **Frontend** | `oraculo/index.html` | Autocontenido. Input, generador de hexagrama, display de resultado, journal, audio |
| **Hexagrama** | Cliente | 6 líneas yin/yang generadas por hash de la pregunta. Línea mutante parpadea con CSS |
| **Audio** | WebAudio | Gong oracular de 5 armónicos al consultar |
| **Journal** | localStorage | Consultas persisten con fecha, pregunta, hexagrama, dictamen, interpretación |
| **Fallback** | Cliente | 64 hexagramas del I Ching con interpretación local |

### Integración en el OS:

| Elemento | Detalle |
|----------|---------|
| Ícono escritorio | `⯁ ORACULO` (ámbar) |
| Menú Inicio | `⯁ ORACULO (I Ching + VALIS)` |
| VOMIT.SH | `oraculo`, `oracle`, `iching`, `mutacion`, `mutación`, `hexagrama` |
| Ventana | 620x750px |

### UX — Conversación contextual:

| Feature | Detalle |
|---------|---------|
| **Input sticky abajo** | Textarea + botón fijos abajo, consultas scrolleables arriba |
| **Tarjetas de conversación** | Cada consulta renderiza como tarjeta con hexagrama → pregunta → dictamen → imagen → interpretación expandible |
| **Session history** | `sessionHistory[]` acumula {question, hexNumber, hexName, judgment, interpretation} |
| **Contexto entre consultas** | El modelo recibe historial completo de la sesión + prompt reforzado: "DEBES mencionar explícitamente el hexagrama previo y conectar ambas lecturas" |
| **Auto-scroll suave** | `.sess-body.scrollIntoView({behavior:'smooth'})` al completar cada consulta |
| **Interpretación expandible** | Colapsada por defecto, clic para expandir |
| **Loading inline** | Spinner dentro de la tarjeta mientras la API responde |

---

## Commits del día (14 en total)

```
66f9048 ORACULO: scroll to dictamen body, include interpretation in session context, strengthen thread continuity prompt
4d93b83 ORACULO: scroll to hexagram element instead of container bottom
590305b ORACULO: smooth auto-scroll to new response cards
346e870 ORACULO: sticky input at bottom, session context between consultations, conversation cards
78bfd08 Add ORACULO.exe: I Ching + VALIS oracle with API, hexagram engine, journal, audio, OS integration
bcb0b2b music: 5 new punk tracks (Conflict, Subhumans, Dystopia, Antisect, His Hero Is Gone)
65b391f Increase choice label/note limits: 48→80, 80→120 chars
154b67f Add 500ms dramatic pause before live node reveal via revealLiveNode()
034bb80 Increase response token/chars limits, add psycho-oracular loading UX with void pulse, oracle phrases, and pre-text glitch
1db35da Enforce español neutro (no voseo) in all prompts, add drum layer + extreme glitch drop attenuation
7cfa88d Redesign audio engine: LFO breathing drone, gong choice, mood-specific sfx, auto-scroll to question
6e126a8 Fix TDZ error: change let to var for audio engine declarations
2c508f0 Add oracular glitch audio engine to VOID_GLITCH: drone, sfx transitions, sound toggle
7ae68b2 Fix syntax error in api/void-glitch.js: unclosed template literal breaking Vercel build
e10defc Show model identity in VOID_GLITCH UI: kicker, status bar, typing indicator, transparent fallback
c8f2ef0 Integrate VOID_GLITCH into OS: desktop icon, Start Menu, VOMIT.SH commands
```
